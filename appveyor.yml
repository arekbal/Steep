version: 'build-{build}'

branches:
  only:
    - master
    - dev

  except:
    - gh-pages
    - dev-benchmarks
    - master-benchmarks

configuration: RELEASE

build_script: 
  - ps: .\build.ps1

# TODO: add a) running benches, b) deploying them to github repo 
